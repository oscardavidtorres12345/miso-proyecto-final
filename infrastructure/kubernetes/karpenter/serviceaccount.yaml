apiVersion: v1
kind: ServiceAccount
metadata:
  name: karpenter
  namespace: karpenter
  annotations:
    # Reemplazar con el output de Terraform: terraform output karpenter_controller_role_arn
    eks.amazonaws.com/role-arn: "REPLACE_WITH_KARPENTER_CONTROLLER_ROLE_ARN"
