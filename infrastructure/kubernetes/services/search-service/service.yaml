apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: travelhub
  labels:
    app: search-service
spec:
  selector:
    app: search-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP

